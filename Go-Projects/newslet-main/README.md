# Newslet

## Generate proto stubs
If you run the scripts for the first time be sure to change the mode of the script
```
chmod +x scripts/protogen.sh
```
To generate the stubs run the following command
```
./scripts/protogen.sh ./api/proto
```